[defaults]
inventory = inventory.ini
roles_path = roles
retry_files_enabled = False
# YAML-formatted output. This used to be `stdout_callback = yaml`, which came
# from community.general and was removed in that collection's v12 — a fresh
# install pulls the current collection, so use the core option instead.
stdout_callback = default
callback_result_format = yaml
nocows = True
interpreter_python = auto_silent
# Facts are read as ansible_facts['os_family'], not the injected top-level
# ansible_os_family. Injection is deprecated and goes away in ansible-core
# 2.24; turning it off now silences the warning and makes any reference we
# missed fail loudly instead of quietly working until the removal.
inject_facts_as_vars = False
